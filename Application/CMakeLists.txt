INCLUDE_DIRECTORIES(${MIDASCPP_SOURCE_DIR}/Code)
SUBDIRS(MIDAScli)

SET(BUILD_MIDAS_DESKTOP OFF CACHE BOOL "Build MIDASDesktop GUI")

IF(BUILD_MIDAS_DESKTOP)
  SUBDIRS(MidasDesktop)
ENDIF(BUILD_MIDAS_DESKTOP)

